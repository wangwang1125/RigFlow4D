# RigFlow4D core runtime.
#
# This file is intentionally small. The previous file was a full pip freeze from
# a ModelArts/TripoSG environment and included local wheel paths, CUDA component
# wheels, git installs, and legacy packages that block clean cloud setup.
#
# For GPU training, use a cloud image with PyTorch preinstalled or install the
# CUDA-matched PyTorch wheel recommended by your provider before this file.

numpy>=1.24,<2.0
torch>=2.2
tqdm>=4.66
PyYAML>=6.0

# DINOv3/HuggingFace visual-token extraction. Weights are downloaded at runtime
# by from_pretrained(...) into the HuggingFace cache or the user-provided cache.
transformers>=4.40
huggingface-hub>=0.23
safetensors>=0.4.5
pillow>=10.0
